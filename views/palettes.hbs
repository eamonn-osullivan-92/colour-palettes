<div class="container">
    {{#each palettes}}
        <div class="palette">
            {{#each colors}}
            <div class="color" style="background-color:{{this}}">{{this}}</div>
            {{/each}}
        </div>
        {{/each}}
</div>